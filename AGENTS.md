# Agent Verification Rules

Before finalizing any changes, the agent must ensure the following commands succeed:

- `npm run verify`

If any command fails, the agent must stop and fix the issue before continuing. PEPE
